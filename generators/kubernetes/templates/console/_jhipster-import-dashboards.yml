apiVersion: batch/v1
kind: Job
metadata:
  name: jhipster-import-dashboards
  namespace: default
spec:
  template:
    metadata:
      name: jhipster-import-dashboards
    spec:
      containers:
      - image: <%= DOCKER_JHIPSTER_IMPORT_DASHBOARDS %>
        name: jhipster-import-dashboards
      restartPolicy: Never
